server.servlet.context-path=/awesome
spring.jpa.open-in-view=false

# Profile activation must be injected from the runtime environment, for example:
#   --spring.profiles.active=local
#   SPRING_PROFILES_ACTIVE=stage
